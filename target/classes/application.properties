# Server port is arbitrary
server.port=8082

# Data Source Setup
spring.datasource.url=jdbc:mysql://localhost:3306/critter
spring.datasource.username=sasa
spring.datasource.password=sasa1234

com.udacity.datasource.username=sasa
com.udacity.datasource.password=sasa1234

spring.datasource.initialization-mode=always
spring.jpa.hibernate.ddl-auto=create-drop

# Note: Users must create a mysql db that they will reference here,
#   create a user that has access to it on localhost
#   and provide the credentials below to access it

# uncomment the below lines to view sql output in console
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

